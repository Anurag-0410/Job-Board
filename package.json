{
  "name": "job-portal",
  "version": "1.0.0",
  "description": "A comprehensive job portal with user roles and authentication, job posting and listing features, application management, and responsive UI design.",
  "main": "server/server.js",
  "scripts": {
    "client": "npm start --prefix client",
    "server": "node server/server.js",
    "dev": "concurrently \"npm run client\" \"npm run server\"",
    "build": "npm run build --prefix client",
    "test": "echo \"No tests specified\" && exit 0"
  },
  "dependencies": {
    "concurrently": "^6.0.0",
    "express": "^4.17.1",
    "mongoose": "^5.10.9",
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "jsonwebtoken": "^8.5.1",
    "bcryptjs": "^2.4.3"
  },
  "devDependencies": {
    "nodemon": "^2.0.4"
  },
  "author": "Your Name",
  "license": "MIT"
}